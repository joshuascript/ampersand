# Build output. Everything here is regenerated by `dotnet build`, and bin/
# carries Avalonia's native payload for every RID - well over 100 MB.
bin/
obj/

# Editor / OS noise
.vs/
.idea/
*.user
*.swp
.DS_Store
